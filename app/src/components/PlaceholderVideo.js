import React from 'react';
import { Placeholder } from 'semantic-ui-react';

const PlaceholderVideo = ({ style }) => (
    <Placeholder style={ style }>
      <Placeholder.Image />
    </Placeholder>
);

export default PlaceholderVideo
